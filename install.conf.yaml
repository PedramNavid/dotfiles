- defaults:
    link:
      relink: true

- clean: ['~']

- create:
    - ~/.zsh
    - ~/bin

- link:
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.config/starship.toml: starship.toml
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/bin/tmux_session:  bin/tmux_session
    ~/.config/awesome: awesome
    ~/.config/picom.conf: picom.conf
